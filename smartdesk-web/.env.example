# Gateway API base URL (browser calls gateway only)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001/api/v1

# Use mock data when gateway is not ready (set to false for real API)
NEXT_PUBLIC_USE_MOCK=true
