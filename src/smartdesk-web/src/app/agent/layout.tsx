/**
 * /agent 布局
 */

export const metadata = {
  title: '坐席工作台 - SmartDesk',
  description: 'SmartDesk 坐席工作台',
};

export default function AgentLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
