/**
 * /portal 布局
 */

export const metadata = {
  title: '报单人门户 - SmartDesk',
  description: 'SmartDesk 报单人门户',
};

export default function PortalLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
