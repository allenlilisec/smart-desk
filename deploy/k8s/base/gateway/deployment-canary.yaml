apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-canary
  namespace: smartdesk-canary
  labels:
    app: gateway
    version: canary
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gateway
      version: canary
  template:
    metadata:
      labels:
        app: gateway
        version: canary
    spec:
      containers:
        - name: gateway
          image: smartdesk/gateway:canary
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: smartdesk-config
          env:
            - name: SERVICE
              value: "gateway"
            - name: VERSION
              value: "canary"
            - name: PORT
              value: "3000"
          livenessProbe:
            httpGet:
              path: /healthz
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5
