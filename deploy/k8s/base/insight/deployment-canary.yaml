apiVersion: apps/v1
kind: Deployment
metadata:
  name: insight-canary
  namespace: smartdesk-canary
  labels:
    app: insight
    version: canary
spec:
  replicas: 1
  selector:
    matchLabels:
      app: insight
      version: canary
  template:
    metadata:
      labels:
        app: insight
        version: canary
    spec:
      containers:
        - name: insight
          image: smartdesk/insight:canary
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: smartdesk-config
          env:
            - name: SERVICE
              value: "insight"
            - name: VERSION
              value: "canary"
            - name: PORT
              value: "8080"
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
