# 规格一致性校验报告

> **校验范围**：SmartDesk 项目 spec↔plan↔tasks 跨文档一致性
> **校验日期**：2026-06-16
> **校验人**：方通（PMO/项目协调员）
> **底稿来源**：SUP-45（P3 /analyze 报告原文）、SUP-261（P4 看板巡检底稿）
> **入仓维护**：秦诺（契约设计），2026-06-17，落地 SUP-268（H-1/H-2 裁决 + P3 报告补入仓）

---

> ## 裁决闭环状态（SUP-260 终裁 → SUP-268 落地）
>
> 本报告原 P3 阶段产出，此前仅存在于 SUP-45 评论、**未入仓**（GAP-0）。现按 SUP-268 补提交至 `specs/`，作为唯一事实源；两项 HIGH 已由架构（梁栋）在 SUP-260 终裁、本 PR 落地闭环：
>
> | 项 | 裁决/落地结论 | 状态 |
> |---|---|---|
> | **H-1** core.yaml 版本漂移 | 以契约 `core.yaml` **v1.1.0** 为准（附加字段 = SemVer minor bump，1.1.0 比 1.0.1 更正确），回填 `specs/core/tasks.md`；不改 yaml | ✅ 已裁决/已闭环 |
> | **H-2** insight.yaml 未冻结 | `insight.yaml` 去 `-draft`，正式定版 **v1.0.0**；tasks.md 措辞对齐 | ✅ 已裁决/已闭环 |
> | **M-3** 跨模块硬阻塞（classification_suggested 事件 schema） | 与 H-2 同源，本 PR 同步固化 `ClassificationSuggestedPayload`（version=1，纳入 v1.0.0 冻结范围），core Phase11 写回获稳定契约 | ✅ 本 PR 同步解除 |

---

## 1. 覆盖完整性检查

### 1.1 Functional Requirements 覆盖情况

| 需求ID | 覆盖状态 | 对应Tasks | 备注 |
|---|---|---|---|
| **FR-001** (gateway认证/RBAC/限流) | ✅ 完整覆盖 | gateway: GW-1(认证), GW-2(RBAC), GW-4(限流); core: T011(鉴权中间件) | 覆盖完整，含红线测试 |
| **FR-002** (工单全生命周期) | ✅ 完整覆盖 | core: CORE-A1(状态机), A2(分派), A3(SLA), B1(评论), B2(附件), B3(查询), B4(合并); gateway: GW-3b; web: WEB-1/2 | 八态状态机完整覆盖 |
| **FR-003** (AI异步分析) | ✅ 完整覆盖 | insight: INS-2/3(分类定级), INS-4(相似); core: Phase11(写回消费); gateway: GW-3c; web: WEB-2(T-02-8/9) | 异步写回路径完整 |
| **FR-004** (领域事件) | ✅ 完整覆盖 | core: T013(事件发布); insight: INS-1(事件消费); gateway: GW-3(转发) | 事件总线路径完整 |
| **FR-005** (转派/升级/@提及) | ✅ 完整覆盖 | core: CORE-A2(分派), B1(评论+@); gateway: GW-3b; web: T-02-3 | 协作能力完整 |
| **FR-006** (看板与质量报表) | ✅ 完整覆盖 | insight: INS-5(统计聚合); gateway: GW-3d; web: WEB-4(看板) | M3能力覆盖 |
| **FR-007** (三端前端) | ✅ 完整覆盖 | web: WEB-1(门户), WEB-2(工作台), WEB-3(管理后台) | 三端完整 |
| **FR-008** (降级) | ✅ 完整覆盖 | core: T022; insight: T013, T027; gateway: T059; web: T-QA-4 | 降级测试完整 |
| **FR-009** (多租户预留) | ✅ 完整覆盖 | core: T005~T007(org_id字段); gateway: D4裁决 | 一期预留不实现隔离 |
| **FR-010** (合规) | ✅ 完整覆盖 | core: T049(M4预留) | M4前闭环，不阻塞M2/M3 |

### 1.2 User Stories 覆盖情况

| Epic | 故事数 | 覆盖状态 | 主要任务分布 |
|---|---|---|---|
| **F1** 认证与权限 | 3 (US-1.1~1.3) | ✅ 完整覆盖 | gateway: GW-1/2/4; web: T-01-1 |
| **F2** 工单生命周期 | 8 (US-2.1~2.8) | ✅ 完整覆盖 | core: CORE-A1~B4; gateway: GW-3b; web: WEB-1/2 |
| **F3** 智能分析 | 4 (US-3.1~3.4) | ✅ 完整覆盖 | insight: INS-2/3/4/5; core: Phase11; gateway: GW-3c; web: WEB-2/4 |
| **F4** 通知与事件 | 4 (US-4.1~4.4) | ✅ 完整覆盖 | insight: INS-1/6/7; gateway: GW-3d; web: T-01-7 |
| **F5** 协作 | 3 (US-5.1~5.3) | ✅ 完整覆盖 | core: CORE-A2/B1; gateway: GW-3b; web: T-02-3/5 |
| **F6** 看板与报表 | 4 (US-6.1~6.4) | ✅ 完整覆盖 | insight: INS-5; gateway: GW-3d; web: WEB-4 |
| **F7** 三端前端 | 3 (US-7.1~7.3) | ✅ 完整覆盖 | web: WEB-1/2/3 |

**结论**：所有 Functional Requirements 和 User Stories 均已在四模块 tasks.md 中有对应任务覆盖，覆盖率 **100%**。

---

## 2. 发现的问题（按严重程度分类）

### 2.1 CRITICAL（阻塞P4）

**暂无 CRITICAL 级别问题**。

---

### 2.2 HIGH（需立即闭环）

#### H-1: core.yaml 契约版本漂移 【✅ 已裁决/已闭环 — SUP-260 终裁，SUP-268 落地】

**问题描述**：
- `specs/core/tasks.md` §0.1 声明契约版本为 `v1.0.1`
- 但实际 `src/openapi/core.yaml` `info.version` 为 **`1.1.0`**
- tasks.md §0.3 已标注为 D-1 drift，但需确认是否已修复

**影响范围**：
- core 实现需以 1.1.0 为准（包含 `Ticket/TicketDetail` 的 `csat_comment`/`csat_rated_at` 字段、`Attachment` 的 `comment_id` 字段）
- 文档版本号滞后，非契约冲突（纯附加字段）

**处置建议**：
- 以 core.yaml v1.1.0 为准实现
- tasks.md 版本引用建议回填为 1.1.0（文档级，交资料/架构，不阻塞编码）
- **责任人**：石磊（core Leader）确认并更新文档引用

**闭环结论（SUP-260 终裁 / SUP-268 落地）**：以契约 `core.yaml` **v1.1.0** 为唯一事实源——新增字段为可选附加、向后兼容，按 SemVer 应为 minor bump，故 1.1.0 比 tasks.md 现写的 1.0.1 更正确；**不改 yaml，改文档**。`specs/core/tasks.md` 全部 `1.0.1` 引用（§0.1/§0.3 D-1/T027/T038/门禁表）已统一回填 1.1.0，D-1 条目已补「1.1.0 为 1.0.1 之后再次附加字段后的 minor 版本」消歧。**已闭环。**

---

#### H-2: insight.yaml 版本未正式冻结 【✅ 已裁决/已闭环 — SUP-260 终裁，SUP-268 落地】

**问题描述**：
- `specs/insight/tasks.md` §0.1 声明契约"已冻结"
- 但实际 `src/openapi/insight.yaml` `info.version` 为 **`v1.0.0-draft`**（含 draft 标记）
- 版本未正式冻结

**影响范围**：
- insight 实现依赖契约稳定性
- 跨模块依赖（core 消费 `insight.classification_suggested`）需对齐字段契约

**处置建议**：
- 确认 insight.yaml 是否已正式冻结（去除 draft 标记）
- 若未冻结，需由梁栋（架构 Leader）确认冻结后方可推进
- **责任人**：苏睿（insight Leader）+ 梁栋（架构裁决）

**闭环结论（SUP-260 终裁 / SUP-268 落地）**：insight 详设 v1.0 已冻结、tasks.md 已按冻结消费、契约内容自洽（classification/similarity/stats/notifications/feedback/events 全覆盖，无 TODO/待定），具备冻结条件。`src/openapi/insight.yaml` 已去 `-draft`，正式定版 **v1.0.0**；`specs/insight/tasks.md` 措辞与之一致（「已冻结」）。**已闭环。**

---

### 2.3 MEDIUM（可记录在P4中跟踪）

#### M-1: gateway D-1 drift 未确认修复

**问题描述**：
- `specs/002-smartdesk-gateway/tasks.md` §0.3 D-1：`GET /auth/me` 响应仍含 `org_id`
- gateway.yaml `Me` schema 已按 D4 裁决删除 `org_id`
- drift 修复任务 T014 已标注，但需确认是否已执行

**处置建议**：
- 确认 T014 是否已合入并验证
- **责任人**：关山（gateway Leader）

---

#### M-2: insight D-2 drift 待落地

**问题描述**：
- `specs/insight/tasks.md` §0.3 D-2：`app/schemas.py` 的 `insight.classification_suggested` payload 未固化
- 写回事件格式漂移

**处置建议**：
- INS-1 实现时（T009）按 OQ-4 嵌套 `PredictResult` 发布，与 `core.yaml` 消费侧对齐
- **责任人**：苏睿（insight Leader）

---

#### M-3: 跨模块阻塞项未显式标注解决时间 【✅ 本 PR 同步解除 — SUP-268】

**问题描述**：
- `specs/core/tasks.md` §跨模块契约依赖：Phase 11 写回消费（M3）阻塞于 insight `insight.classification_suggested` 事件 schema 冻结
- `specs/insight/tasks.md` §依赖与执行顺序：标注 O-1/O-3/O-6 跨模块阻塞，但未显式标注解决时间

**处置建议**：
- 由 PMO（程远）协调 insight Leader（苏睿）与架构（梁栋）确认 insight.yaml 冻结时间
- **责任人**：程远（PMO Leader）

**闭环结论（SUP-268）**：与 H-2 同源。此前 `insight.classification_suggested` 的 payload 被 park 到 M4（DomainEvent 映射表「M4 固化，暂为自由 object」+ oneOf 自由 object），是 M-3 的真阻塞点。本 PR 已在 `insight.yaml` 提前固化 `ClassificationSuggestedPayload`（version=1，附加-only 演进规则同 DomainEvent §337-338，字段：`predicted_category_id`/`confidence`/`priority`/`applied`），并纳入 v1.0.0 冻结范围；payload 映射表已为该 event 单列、oneOf 已新增对应 `$ref`。core Phase11 写回自此获得稳定契约。字段集为架构终裁锁定，如与 insight 详设或 core 实际消费有出入，回 SUP-268 由梁栋裁决。**M3 硬阻塞解除。**

---

### 2.4 LOW（文档级，不阻塞）

#### L-1: 部分任务未显式标注 US 编号

**问题描述**：
- 少数任务（如 gateway T029~T036 客户端基建）未显式标注 `[USn]`
- 但可从上下文推断归属

**处置建议**：
- 文档级改进，不影响实现
- 后续 tasks.md 维护时可补充标注

---

#### L-2: 任务编号体系不一致

**问题描述**：
- core/insight/gateway 使用 `T001`~`T049` 编号
- web 使用 `T-00-1`~`T-05-2` 编号（带模块前缀）

**处置建议**：
- 可接受差异，不影响实现
- 建议 future 统一编号体系（文档改进）

---

## 3. 覆盖统计

### 3.1 需求覆盖统计

| 类型 | 总数 | 已覆盖 | 覆盖率 |
|---|---|---|---|
| **Functional Requirements** | 10 | 10 | **100%** |
| **User Stories** | 29 | 29 | **100%** |
| **PRD裁决(OQ)** | 11 | 11 | **100%** |
| **契约决策(D)** | 5 | 5 | **100%** |

### 3.2 任务统计

| 模块 | Phase数 | 总任务数 | 关键红线任务数 |
|---|---|---|---|
| **core** | 12 | 49 | 5（幂等/状态机/越权/降级/契约） |
| **insight** | 7 | 42 | 3（契约/准确率/降级） |
| **gateway** | 11 | 66 | 4（越权/429/契约/D-1修复） |
| **web** | 5+QA | ~50 | 5（越权E2E/降级/Lighthouse） |
| **合计** | — | **~207** | **17** |

---

## 4. 术语一致性检查

### 4.1 状态机术语

| 中文（PRD/spec） | 英文（契约/tasks） | 一致性 |
|---|---|---|
| 新建 | `new` | ✅ |
| 受理 | `accepted` | ✅ |
| 处理中 | `in_progress` | ✅ |
| 待用户 | `pending_user` | ✅ |
| 已解决 | `resolved` | ✅ |
| 已关闭 | `closed` | ✅ |
| 挂起 | `suspended` | ✅ |
| 取消 | `cancelled` | ✅ |

**结论**：八态术语在 spec/plan/tasks/契约中完全一致。

---

### 4.2 模块/服务命名

| 命名 | spec/plan/tasks | 契约 | 一致性 |
|---|---|---|---|
| gateway | `smartdesk-gateway` | `smartdesk-gateway` | ✅ |
| core | `smartdesk-core` | `smartdesk-core` | ✅ |
| insight | `smartdesk-insight` | `smartdesk-insight` | ✅ |
| web | `smartdesk-web` | —（前端） | ✅ |

**结论**：模块命名完全一致。

---

## 5. 依赖序一致性检查

### 5.1 Phase 依赖序对齐

| 模块 | tasks.md依赖序 | 系统详设里程碑 | 一致性 |
|---|---|---|---|
| **core** | Phase1→2→3→4→5~11 | M2(Phase1-9), M3(Phase10-11), M4(Phase12) | ✅ 完全对齐 |
| **insight** | Phase1→2→3~7 | M2(Phase1-6), M3(Phase3-5), M4(Phase7) | ✅ 完全对齐 |
| **gateway** | Phase1~11 | M2(Phase1-7), M3(Phase8-9), M4(Phase10-11) | ✅ 完全对齐 |
| **web** | WEB-0→1/2/3→4→5 | Sprint1-5, M2/M3/M4 | ✅ 完全对齐 |

**结论**：所有模块的 Phase 依赖序与系统详设 §12.2 里程碑完全一致。

---

### 5.2 跨模块依赖标注

| 依赖关系 | tasks.md标注 | 一致性 |
|---|---|---|
| gateway → core契约 | ✅ 已标注（GW-3b阻塞） | ✅ |
| gateway → insight契约 | ✅ 已标注（GW-3c/3d软阻塞） | ✅ |
| core → insight事件schema | ✅ 已标注（Phase11硬阻塞） | ✅ |
| web → gateway契约 | ✅ 已标注（全量依赖） | ✅ |
| insight → core事件schema | ✅ 已标注（INS-1前置） | ✅ |

**结论**：跨模块依赖在 tasks.md 中已显式标注，与系统详设 §12.3 一致。

---

## 6. 契约对齐检查

### 6.1 契约文件路径

| 模块 | tasks.md引用 | 实际路径 | 一致性 |
|---|---|---|---|
| gateway | `src/openapi/gateway.yaml` | ✅ 存在 | ✅ |
| core | `src/openapi/core.yaml` | ✅ 存在 | ✅ |
| insight | `src/openapi/insight.yaml` | ✅ 存在 | ✅ |

**结论**：契约文件路径引用完全正确。

---

### 6.2 契约版本对齐

| 契约文件 | yaml实际版本 | tasks.md引用版本 | 一致性 |
|---|---|---|---|
| **gateway.yaml** | `1.0.1` | `v1.0.1` | ✅ 完全一致 |
| **core.yaml** | `1.1.0` | `v1.1.0`（H-1 已回填） | ✅ 已对齐 |
| **insight.yaml** | `1.0.0`（H-2 已去 draft 定版） | "已冻结 v1.0.0" | ✅ 已对齐 |

> 注：上表为 SUP-268 落地后状态。原 P3 校验时 core.yaml 引用为 `v1.0.1`（漂移，见 H-1）、insight.yaml 为 `1.0.0-draft`（未冻结，见 H-2），均已闭环。

---

### 6.3 契约端点覆盖

| 契约 | 端点数 | tasks.md覆盖 | 一致性 |
|---|---|---|---|
| **gateway.yaml** | 34 operations | GW-3全覆盖 | ✅ |
| **core.yaml** | 17业务path + healthz/readyz | CORE-A1~C全覆盖 | ✅ |
| **insight.yaml** | classification/similarity/stats/notifications/feedback/events | INS-1~7全覆盖 | ✅ |

**结论**：契约端点在 tasks.md 中已全覆盖。

---

## 7. 关键设计决策落地检查

### 7.1 D1-D5 契约决策落地

| 决策 | 裁定内容 | tasks.md落地 | 一致性 |
|---|---|---|---|
| **D1** | 建议写回=纯事件（不新增core同步端点） | core T044（明确不开同步端点） | ✅ 已落地 |
| **D2** | 聚合边界=A（懒加载similar/suggestion） | gateway T055/056; web T-01-4 | ✅ 已落地 |
| **D3** | 事件分区/顺序（ticket_id哈希分区） | insight.yaml固化; core T013 | ✅ 已落地 |
| **D4** | org_id不进对外契约 | gateway T014(修复D-1 drift) | ✅ 已落地（待确认修复） |
| **D5** | csat一期对外补齐；watchers/links暂不对外 | gateway T046; core T027 | ✅ 已落地 |

---

### 7.2 OQ裁决落地

| 裁决 | 内容 | tasks.md落地 | 一致性 |
|---|---|---|---|
| **OQ-1** | SLA优先级驱动/可配/可暂停 | core T031(SLA引擎), T016(策略种子) | ✅ 已落地 |
| **OQ-2** | 自建账号+预留OIDC | gateway GW-1, T019(IdP抽象) | ✅ 已落地 |
| **OQ-4** | 一期建议+人工确认（阈值≥0.85） | insight T021; core T043 | ✅ 已落地 |
| **OQ-5** | 一期关键词+标题/分类 | insight T023(PG全文) | ✅ 已落地 |
| **OQ-7** | 单组织起步+预留多租户 | core T005~T007(org_id) | ✅ 已落地 |
| **OQ-8** | 八态状态机+终态(closed/cancelled) | core T024(八态) | ✅ 已落地 |
| **OQ-9** | 附件≤20MB/白名单/OSS | core T036/T038 | ✅ 已落地 |
| **OQ-10** | 合规预留（M4闭环） | core T049(M4预留) | ✅ 已落地 |
| **OQ-13** | 重开窗口7天 | core T024(reopen≤7天) | ✅ 已落地 |

**结论**：所有关键设计决策（D1-D5、OQ-1~13）均已在 tasks.md 中显式落地，无遗漏。

---

## 8. 结论

### 8.1 可推进P4判定

✅ **可以推进 P4（实现阶段）**。原列两项 HIGH 已闭环：

1. **H-1 core.yaml版本漂移** ✅ 已闭环：以 v1.1.0 为准实现，`specs/core/tasks.md` 引用已回填 1.1.0（SUP-260 终裁 / SUP-268 落地）。
2. **H-2 insight.yaml未冻结** ✅ 已闭环：insight.yaml 去 draft，正式定版 v1.0.0（SUP-260 终裁 / SUP-268 落地）。

---

### 8.2 风险提示

⚠️ **跨模块硬阻塞（M-3）已解除**：
- core Phase 11（写回消费）原阻塞于 insight `insight.classification_suggested` 事件 schema 冻结
- SUP-268 已在 insight.yaml v1.0.0 内提前固化 `ClassificationSuggestedPayload`，core Phase11 获得稳定契约
- 字段集为架构终裁锁定；如与 insight 详设或 core 实际消费有出入，回 SUP-268 由梁栋裁决

---

### 8.3 整体评价

| 维度 | 评价 | 说明 |
|---|---|---|
| **覆盖完整性** | ✅ 优秀 | FR/US/D/OQ 全覆盖，覆盖率100% |
| **术语一致性** | ✅ 优秀 | 状态机/模块命名完全一致 |
| **依赖序一致性** | ✅ 优秀 | Phase依赖与里程碑完全对齐 |
| **契约对齐** | ✅ 优秀 | 原 2 处版本漂移（H-1/H-2）已闭环 |
| **决策落地** | ✅ 优秀 | 所有设计决策显式落地 |

---

### 8.4 下一步行动

1. **HIGH 闭环** ✅ 已完成：
   - core.yaml v1.1.0 确认 + tasks.md 引用回填（SUP-268）
   - insight.yaml 去 draft 定版 v1.0.0（SUP-268）

2. **P4启动前确认（MEDIUM）**：
   - 关山确认 gateway D-1 drift 修复（T014）
   - ~~程远协调 insight.yaml 冻结时间~~ ✅ 已冻结
   - M-3 跨模块硬阻塞已随 SUP-268 解除

3. **文档改进（LOW）**：
   - 补充部分任务的 US 编号标注
   - 统一任务编号体系（future）

---

## 附录：校验方法

- **文档来源**：spec.md、plan.md、系统详设、四模块tasks.md、PRD、用户故事、三份契约yaml
- **校验工具**：人工逐条对比，grep 搜索关键术语
- **校验时间**：2026-06-16
- **校验覆盖**：全覆盖（100%）
